<% include ../common/index-top.ejs %>
        <article>
            <img src="images/icon/icon_label.png">
            <div id="title">
                By class
            </div>
            <div class="ui compact menu">
                <div class="ui simple dropdown item" id="head">
                    <%= defaultDate %>
                    <i class="dropdown icon"></i>
                    <div class="menu">
                        <% for(i in dates){ %>
                            <div class="item date"><%= dates[i] %></div>
                        <% } %>
                    </div>
                </div>
            </div>
            <canvas id="classChart" width="700" height="350"></canvas>
        </article>
        <% include ./script.ejs %>
<% include ../common/index-bottom.ejs %>